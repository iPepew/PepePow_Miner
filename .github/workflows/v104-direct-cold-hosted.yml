name: v1.0.4 V100 direct-cold hosted build

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/v104-direct-cold-hosted.yml"
  workflow_dispatch:

permissions:
  contents: write

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    container:
      image: nvidia/cuda:12.8.1-devel-ubuntu22.04
    env:
      RELEASE_TAG: hiveos-v100-v104-direct-cold-test
      ROOT: PepeW-Miner
      ASSET: PepeW-Miner-HiveOS.tar.gz

    steps:
      - name: Checkout direct-cold candidate
        uses: actions/checkout@v4
        with:
          ref: agent/v100-v104-direct-cold

      - name: Install build tools
        run: |
          set -Eeuo pipefail
          apt-get update
          DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            build-essential ca-certificates git ninja-build python3 python3-pip tar gzip file binutils gh
          python3 -m pip install --no-cache-dir "cmake==3.31.6"
          rm -rf /var/lib/apt/lists/*
          /usr/local/cuda/bin/nvcc --list-gpu-code | grep -F sm_70

      - name: Configure exact v1.0.4 direct-cold candidate
        run: |
          set -Eeuo pipefail
          test "$(tr -d '\r\n' < VERSION)" = "1.0.4"
          export CUDACXX=/usr/local/cuda/bin/nvcc
          cmake -S native -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_CUDA_COMPILER=/usr/local/cuda/bin/nvcc \
            -DPEPEPOW_ENABLE_CUDA=ON \
            -DPEPEPOW_BUILD_TESTS=ON \
            -DPEPEPOW_CUDA_PTXAS_VERBOSE=ON \
            -DPEPEPOW_CUDA_ARCHITECTURES="70-real" \
            -DPEPEPOW_CUDA_THREADS=736 \
            -DPEPEPOW_CUDA_MIN_BLOCKS=1

      - name: Build and validate
        run: |
          set -Eeuo pipefail
          cmake --build build --parallel 4 --target \
            pepepowminer pepepow_core_tests pepepow_cuda_tests \
            pepepow_cuda_header80_validation pepepow_header80_benchmark 2>&1 | tee /tmp/build.log
          /usr/local/cuda/bin/cuobjdump --list-elf build/pepepowminer | tee /tmp/elf.txt
          grep -F sm_70 /tmp/elf.txt
          ! grep -Eq 'sm_(61|75|86|89|120)' /tmp/elf.txt
          strings -a build/pepepowminer | grep -F 'POOL_REFERENCE hoohash=V110 matrix_seed=BLAKE3_MASKED_HEADER header_nonce=BE32 mix_nonce=LE32 submit_nonce=LE_HEX share_target=NBITS_DIV_DIFFICULTY'
          strings -a build/pepepowminer | grep -F 'cuda-header80-monolithic-pipeline'
          ctest --test-dir build --output-on-failure

      - name: Package
        run: |
          set -Eeuo pipefail
          rm -rf stage dist
          mkdir -p "stage/${ROOT}" dist
          install -m 0755 build/pepepowminer "stage/${ROOT}/pepepowminer"
          install -m 0755 hiveos/h-config.sh "stage/${ROOT}/h-config.sh"
          install -m 0755 hiveos/h-run.sh "stage/${ROOT}/h-run.sh"
          install -m 0755 hiveos/h-stats.sh "stage/${ROOT}/h-stats.sh"
          install -m 0755 hiveos/stratum-replay-proxy.py "stage/${ROOT}/stratum-replay-proxy.py"
          install -m 0644 hiveos/h-manifest.conf "stage/${ROOT}/h-manifest.conf"
          sed -i 's/^CUSTOM_NAME=.*/CUSTOM_NAME=PepeW-Miner/' "stage/${ROOT}/h-manifest.conf"
          sed -i 's/^CUSTOM_VERSION=.*/CUSTOM_VERSION=1.0.4-sm70-direct-cold/' "stage/${ROOT}/h-manifest.conf"
          cat > "stage/${ROOT}/BUILD_INFO.txt" <<EOF
          version=1.0.4-sm70-direct-cold
          source_behavior=release/v1.0.4
          candidate_branch=agent/v100-v104-direct-cold
          cuda_toolkit=12.8.1
          cuda_architectures=70-real
          cuda_native=sm_70
          cuda_profile=service736-direct-cold
          threads=736
          min_blocks=1
          semantics=exact-release-v1.0.4-source
          pool_reference=NBITS_DIV_DIFFICULTY
          header_nonce=BE32
          mix_nonce=LE32
          submit_nonce=LE_HEX
          matrix_seed=BLAKE3_MASKED_HEADER
          optimization=remove_per_nibble_block_barrier_and_shared_queue
          promotion=forbidden-until-real-accepted-shares
          EOF
          tar --sort=name --owner=0 --group=0 --numeric-owner -C stage -cf - "${ROOT}" | gzip -n > "dist/${ASSET}"
          sha256sum "dist/${ASSET}" > "dist/${ASSET}.sha256"

      - name: Publish isolated prerelease
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -Eeuo pipefail
          notes='Exact v1.0.4 behavior on sm_70 with direct per-thread cold-path evaluation. Pool/consensus semantics unchanged. Hardware promotion requires accepted_delta > 0.'
          if gh release view "$RELEASE_TAG" --repo "$GH_REPO" >/dev/null 2>&1; then
            gh release upload "$RELEASE_TAG" "dist/${ASSET}" "dist/${ASSET}.sha256" --repo "$GH_REPO" --clobber
          else
            gh release create "$RELEASE_TAG" "dist/${ASSET}" "dist/${ASSET}.sha256" --repo "$GH_REPO" --title "PepeW v1.0.4 V100 direct-cold" --notes "$notes" --prerelease
          fi
          echo "candidate=https://github.com/${GITHUB_REPOSITORY}/releases/download/${RELEASE_TAG}/${ASSET}"
