name: V100 no-barrier workflow smoke

on:
  push:
    branches:
      - agent/v21-v100-nobarrier-direct
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: echo workflow-ok
